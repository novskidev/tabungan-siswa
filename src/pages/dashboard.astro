---
import { getCurrentProfile } from '../lib/supabase';

const user = await getCurrentProfile(Astro);
if (!user) return Astro.redirect('/login');
return Astro.redirect(user.role === 'teacher' ? '/guru' : '/orangtua');
---
